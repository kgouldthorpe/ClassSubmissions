create table owners_table(
id INT not null Primary Key
owner_name varchar(235));

create table pet_names(
id int not null primary key
owner_id int not null
pet_name varchar(235)
type varchar(type));

create table service(
)
